from pathlib import Path
p=Path('Assets/scripts/WeaponIdleSynchronizer.cs');s=p.read_text(encoding='utf-8-sig');s=s.replace('public string id;\n        public AnimancerComponent animator;', 'public string id;\n        public WeaponAudioProfile audioProfile;\n        public AnimancerComponent animator;');s=s.replace('public Transform RightGrip =>', 'public WeaponAudioProfile AudioProfile => currentWeapon != null && currentWeapon.audioProfile != null ? currentWeapon.audioProfile : GameAudio.Profile(WeaponId);\n    public Transform RightGrip =>');p.write_text(s,encoding='utf-8')
p=Path('Assets/scripts/PlayerOwnershipSetup.cs');s=p.read_text();s=s.replace('if (GetComponent<WeaponAmmo>() == null)', 'if (GetComponent<PlayerAudio>() == null) gameObject.AddComponent<PlayerAudio>();\n        if (GetComponent<WeaponAmmo>() == null)');s=s.replace('playerCamera.enabled = local;', 'playerCamera.enabled = local;\n            if (local && playerCamera.GetComponentInChildren<AudioListener>(true) == null) playerCamera.gameObject.AddComponent<AudioListener>();');p.write_text(s)
p=Path('Assets/scripts/NetworkWeapon.cs');s=p.read_text();s=s.replace('public BulletTrail visual;', 'public BulletTrail visual;\n        public string weaponId;');s=s.replace('time = time, visual = tracer };', 'time = time, visual = tracer, weaponId = weaponAnimation != null ? weaponAnimation.WeaponId : "ak74" };');s=s.replace('if (!muzzleFlashEnabled) return;', 'GameAudio.Shot(weaponAnimation != null ? weaponAnimation.AudioProfile : GameAudio.Profile("ak74"), start, sequence,\n            !BotController.IsBot(this) && (!PhotonNetwork.InRoom || photonView.IsMine));\n        if (!muzzleFlashEnabled) return;');s=s.replace('BotController.IsBot(this) ? photonView.ViewID : 0);', 'BotController.IsBot(this) ? photonView.ViewID : 0, flight.weaponId);');s=s.replace('if (flight.visual != null) flight.visual.Impact(point, ShotKey(sequence));','if (flight.visual != null) flight.visual.Impact(point, ShotKey(sequence));\n        if (environmentHit && BulletHitUtility.IsFinite(point)) GameAudio.Effect("Impacts/impact" + (sequence % 3), point, .4f, 30f);');p.write_text(s)
p=Path('Assets/scripts/PlayerHealth.cs');s=p.read_text();s=s.replace('public readonly string victimName;', 'public readonly string weaponId;\n        public readonly string victimName;');s=s.replace('int victimTeam, int killerTeam, int assisterTeam)', 'int victimTeam, int killerTeam, int assisterTeam, string weaponId = "")');s=s.replace('this.victimActorNr = victimActorNr;', 'this.weaponId = weaponId;\n            this.victimActorNr = victimActorNr;');s=s.replace('int killerBotViewId = 0)', 'int killerBotViewId = 0, string weaponId = "")');s=s.replace('killerActorNr, assisterActorNr, killerBotViewId);','killerActorNr, assisterActorNr, killerBotViewId, weaponId);');s=s.replace('killerActorNr, assisterActorNr, killerBotViewId }', 'killerActorNr, assisterActorNr, killerBotViewId, weaponId ?? "" }');s=s.replace('BuildKillInfo(killerActorNr, assisterActorNr, killerBotViewId)', 'BuildKillInfo(killerActorNr, assisterActorNr, killerBotViewId, weaponId)');s=s.replace('state[6] is int botView ? botView : 0);','state[6] is int botView ? botView : 0, state.Length >= 8 ? state[7] as string ?? "" : "");');s=s.replace('TeamOf(assisterPlayer));','TeamOf(assisterPlayer), weaponId);');p.write_text(s)
p=Path('Assets/scripts/PlayerHUD.cs');s=p.read_text();s=s.replace('killfeedFontSize = 18','killfeedFontSize = 20');s=s.replace('info.killerActorNr <= 0', 'info.killerActorNr == -1 || info.killerActorNr == 0');s=s.replace(' || info.killerName == info.victimName','');s=s.replace('KillfeedTeamColor(info.victimTeam), suicide);','KillfeedTeamColor(info.victimTeam), suicide, GameAudio.WeaponName(info.weaponId));');s=s.replace('killfeedFont = (weaponName != null ? weaponName.font : null) ?? Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf");','killfeedFont = Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf");\n        hudCanvas.pixelPerfect = true;');s=s.replace('new Vector2(420f, 0f)', 'new Vector2(660f, 0f)');s=s.replace('Color victimColor, bool suicide)', 'Color victimColor, bool suicide, string weapon)');s=s.replace('new Color(0f, 0f, 0f, .55f)', 'new Color(.025f, .035f, .045f, .9f)');s=s.replace('"\\u2192", new Color(1f, 1f, 1f, .65f)', '"\\u2192 " + weapon + " \\u2192", Color.white');s=s.replace('label.fontStyle = FontStyle.Bold;', 'label.fontStyle = FontStyle.Normal;\n        label.resizeTextForBestFit = false;');s=s.replace('private void OnRespawnClicked()\n    {','private void OnRespawnClicked()\n    {\n        GameAudio.Effect("UI/click", Vector3.zero, .5f, 1f, true);');s=s.replace('killfeedEntries.Clear();','foreach (var entry in killfeedEntries) if (entry != null) Destroy(entry);\n        killfeedEntries.Clear();');p.write_text(s)
p=Path('Assets/scripts/KillfeedManager.cs');s=p.read_text();s=s.replace('info.assisterName, info.assisterTeam);','info.assisterName, info.assisterTeam, GameAudio.WeaponName(info.weaponId));');s=s.replace('int assisterTeam = 0)', 'int assisterTeam = 0, string weaponName = "Unknown")');s=s.replace('killerActorNr <= 0', '(killerActorNr == -1 || killerActorNr == 0)');s=s.replace(' ||\n                       (!string.IsNullOrEmpty(killerName) && killerName == victimName)', '');s=s.replace('TeamColor(victimTeam), suicide);','TeamColor(victimTeam), suicide, weaponName);');p.write_text(s)
p=Path('Assets/scripts/KillfeedEntry.cs');s=p.read_text();s=s.replace('bool suicide)', 'bool suicide, string weaponName = "Unknown")');s=s.replace('separatorLabel.text = "\\u2192";', 'separatorLabel.text = "\\u2192 " + weaponName + " \\u2192";');p.write_text(s)
